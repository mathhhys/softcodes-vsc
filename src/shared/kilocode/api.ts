import { APP_URLS } from "../../config/constants"

/**
 * Returns the base URL for Softcodes API endpoints
 * This is used for marketplace data, MCP servers, and other Softcodes-specific features
 */
export const getKiloCodeApiUrl = () => APP_URLS.ROO_CODE_API
